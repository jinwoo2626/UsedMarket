<!DOCTYPE html>
 <html>
    <% include ../partials/header %>  <!-- header 불러오기 -->
 <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
    <title>로그인</title>
    <% include ../partials/scripts %>  <!-- 부트스트랩 코드 불러오기 -->
 </head>
 <body>
  <main class="page contact-page">
    <section class="portfolio-block contact">
     <div class="container">
      <div class="heading">
        <h2>로그인</h2>
      </div>
        <form action="/users/login" method="post">
          <div class="mb-3">
              <label class="form-label" for="idemail">아이디</label>
              <input class="form-control item" type="text" name="idemail" required autofocus>
          </div>
          <div class="mb-3">
              <label class="form-label" for="password">비밀번호</label>
              <input class="form-control item" type="password" name="password" required>
          </div>
          <div class="mb-3">
              <button class="btn btn-primary btn-lg d-block w-100" type="submit">로그인</button> <!-- 로그인 -->
          </div>
        </form>
     </div>
    </section>
  </main>
 </body>
 </html>
