<!DOCTYPE html>
 <html>
    <% include ../partials/header %>  <!-- header 불러오기 -->
 <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
    <title>회원정보</title>
    <% include ../partials/scripts %>  <!-- 부트스트랩 코드 불러오기 -->
 </head>
 <body>
  <main class="page contact-page">
    <section class="portfolio-block contact">
      <div class="container">
        <% if (users.length > 0) {%>  <!-- users의 길이가 0 이상일때 / users가 넘어왔을때 -->
        <% users.forEach(user => {%>  <!-- forEach문으로 반복 -->
          <center><h3 class="mb-3">
        <%= user.name %> 님의 회원정보</h3></center>  <!-- 넘어온 값 출력 -->
          <form method="post" action="/users/<%= user._id %>?_method=DELETE">  <!-- form 태그로 경로와 전송방식 결정 -->
             <div class="mb-3">
              <label class="form-label" for="idemail">아이디</label>
              <input class="form-control item" type="text" name="idemail" value="<%= user.idemail %>" readonly> <!-- 넘어온 값 출력 / readonly를 사용하여 값 수정 X -->
             </div>
             <div class="mb-3">
              <label class="form-label" for="name">이름</label>
              <input class="form-control item" type="text" name="name" value="<%= user.name %>" readonly>
             </div>
             <div class="mb-3">
              <label class="form-label" for="phone">전화번호</label>
              <input class="form-control item" type="text" name="phone" value="<%= user.phone %>" readonly>
             </div>
             <div class="mb-3">
              <label class="form-label" for="money">보유금액</label>
              <input class="form-control item" type="text" name="money" value="<%= user.money %>원" readonly>
            </div>   
            <a href="/users/purchaseinfo/<%= user._id %>" class="btn btn-info btn-block  mb-2">구매기록확인</a> <!-- 해당 회원이 구매한 제품 기록 확인 -->
            <a href="/users/updateinfo/<%= user._id %>" class="btn btn-success btn-block  mb-2">회원정보수정</a> <!-- 회원정보 수정 -->        
            <input type="hidden" name="_method" value="DELETE">
            <input type="submit" class="btn btn-danger btn-block float-end" value="회원탈퇴"> <!-- 회원탈퇴 -->
          </form>
        <% }); %>
        <% } else { %>
          <p>회원정보가 없습니다.</p>
        <% } %>
    </div>
  </section>
  </main>
 </body>
 </html>
