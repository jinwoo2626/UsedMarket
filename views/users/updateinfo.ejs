<!DOCTYPE html>
 <html>
    <% include ../partials/header %>  <!-- header 불러오기 -->
 <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no">
    <title>회원정보수정</title>
    <% include ../partials/scripts %>  <!-- 부트스트랩 코드 불러오기 -->
 </head>
 <body>
  <main class="page contact-page">
    <section class="portfolio-block contact">
      <div class="container">
        <% if (users.length > 0) {%>  <!-- users의 길이가 0 이상일때 / users가 넘어왔을때 -->
        <% users.forEach(user => {%>  <!-- forEach문으로 반복 -->
         <center><h3 class="mb-3">회원정보수정</h3></center>
          <form action="/users/<%= user._id %>?_method=PUT" method="post"> <!-- 넘어온 값 출력 --><!-- form 태그로 경로와 전송방식 결정 -->
            <div class="mb-3">
              <label class="form-label" for="name">이름</label>
              <input class="form-control item" type="text" name="name" value="<%= user.name %>">
            </div>
            <div class="mb-3">
              <label class="form-label" for="phone">전화번호</label>
              <input class="form-control item" type="text" name="phone" value="<%= user.phone %>">
            </div>       
             <center><input type="submit" class="btn btn-success btn-block" style="width:300px" value="회원정보수정"></center> <!-- 회원정보 수정 -->
          </form>
        <% }); %>
        <% } else { %>
          <p>회원정보가 없습니다.</p>
        <% } %>
    </div>
  </section>
  </main>
 </body>
 </html>
